---
apiVersion: cdi.kubevirt.io/v1beta1
kind: DataSource
metadata:
  name: rhel9-httpd-custom
  namespace: openshift-virtualization-os-images
  labels:
    instancetype.kubevirt.io/default-preference: rhel.9
    app: rhel9-httpd-custom
  annotations:
    cdi.kubevirt.io/storage.bind.immediate.requested: "true"
spec:
  source:
    registry:
      # Update this URL to match your registry
      url: "docker://quay.io/YOUR_USERNAME/rhel9-httpd-custom:latest"
      # If using a private registry, create a pull secret:
      # pullMethod: node
      # secretRef: my-registry-secret
