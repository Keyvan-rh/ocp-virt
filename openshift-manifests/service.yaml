---
apiVersion: v1
kind: Service
metadata:
  name: centos-stream-9-httpd-test
  namespace: default
spec:
  selector:
    kubevirt.io/vm: centos-stream-9-httpd-test
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: centos-stream-9-httpd-test
  namespace: default
spec:
  to:
    kind: Service
    name: centos-stream-9-httpd-test
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
